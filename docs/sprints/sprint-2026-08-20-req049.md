# Sprint „Operable" — 2026-08-20 → 2026-09-03 (REQ-049)

## Sprint story (goal)

> **A Romanian SMB owner can sign up, set up their company, issue an invoice and
> submit it to ANAF without ever hitting a dead end — and the DocumentIulia team
> has a staff back-office where they see every client, answer every client
> request, and manage every account.**

Two sides of the same coin: the **client side** must be *fully usable* (no broken
links, no fabricated data, no dead-end journeys), and the **staff side** must
exist at all (today there is no console to operate client requests).

**Inputs:** REQ-048 review backlog (63 verified findings, waves 1–3 shipped),
expert-consortium review `wf_5a18e6ba-e30` (7 experts · 93 gaps · 31 blockers — folded in, see C1), CLAUDE.md expert-team protocol.

**Capacity assumption:** autonomous agent, ~55 SP. Must = 39 SP.

---

## EPIC A — Staff Back-Office (`/dashboard/admin`)

*As a DocumentIulia staff member, I need one place to see clients, operate their
requests, and manage their accounts, so client operations stop depending on
direct database access.*

### A1 — Admin backend module · **Must · 8 SP** · ✅ shipped PR #53
*As staff, I can find any client and see their whole situation.*

**To-dos**
- [x] `backend/src/admin/` module: `AdminController` + `AdminService`, guarded `JwtAuthGuard + RolesGuard @Roles(ADMIN)` (staff-wide read; mutations ADMIN-only)
- [x] `GET /admin/overview` — KPIs: users total/new-30d, orgs, invoices count+value 30d, e-Factura submissions, open tickets
- [x] `GET /admin/clients?search=&tier=&role=&page=` — paginated user+org list (email, company, CUI, tier, role, createdAt, lastActivity from AuditLog)
- [x] `GET /admin/clients/:id` — client-360: profile, organization, subscription/tier, last 10 invoices, e-Factura statuses, last errors from error log, open tickets
- [x] `PATCH /admin/clients/:id` — tier/role/isActive changes, **every change writes an AuditLog row**
- [x] Jest: guards deny USER role; 360 aggregates correctly; audit row on patch
- **AC:** staff finds any client by email/CUI in <2 clicks; USER role gets 403 on all `/admin/*`.

### A2 — Client requests (ticket system) · **Must · 8 SP** · ✅ shipped PR #53/#54
*As a client, I can ask for help from my dashboard; as staff, nothing lands in a void.*

**To-dos**
- [x] Prisma: `SupportTicket` (id, userId, subject, category, status `OPEN|IN_PROGRESS|WAITING_CLIENT|RESOLVED|CLOSED`, priority, assignedToId?, createdAt/updatedAt) + `TicketMessage` (ticketId, authorId, isStaff, body, createdAt); migration
- [x] Client endpoints: `POST /support/tickets`, `GET /support/tickets` (own), `POST /support/tickets/:id/messages`, reopen
- [x] Staff endpoints: `GET /admin/tickets?status=&assigned=`, `PATCH /admin/tickets/:id` (status/assign/priority), `POST /admin/tickets/:id/messages` (isStaff=true)
- [x] Wire the public **/contact form** → creates a ticket (guest email captured) instead of going nowhere
- [x] Client UI: „Ajutor / Cererile mele" page — new request + thread view
- [x] Jest on ownership scoping (client sees only own tickets)
- **AC:** contact-form submission appears in the staff inbox; client sees staff reply in their thread; status transitions audited.

### A3 — Staff frontend section · **Must · 8 SP** · ✅ shipped PR #54
*As staff, the console is one place: overview → clients → requests → tools.*

**To-dos**
- [x] Role-gated layout `/[locale]/dashboard/admin` (hide nav entry + client-side redirect for non-staff; server relies on 403s)
- [x] `admin/page.tsx` — overview KPIs + open-ticket count + newest clients
- [x] `admin/clients/page.tsx` — searchable table (email/company/CUI/tier/status)
- [x] `admin/clients/[id]/page.tsx` — client-360 with tier/role editing (confirmation dialog, reason field → audit)
- [x] `admin/requests/page.tsx` — ticket inbox: filter by status/assignee, thread panel, reply, status buttons
- [x] „Tools" rail linking existing staff surfaces: CRM, error-logs page, security audit, monitoring
- [x] RO diacritics correct throughout; loading/empty/error states on every view (no fabricated placeholders)
- **AC:** a staff member resolves a real ticket end-to-end in the UI; a USER never sees the section.

### A4 — CRM as client-operations tooling · **Should · 5 SP**
*As staff, client requests connect to the CRM record of that client.*

**To-dos**
- [ ] Link `User ↔ CRM Contact` (nullable `contactId` on User or lookup-by-email service)
- [ ] "Create CRM activity from ticket" action (staff inbox → activity on the linked contact)
- [ ] Client-360 shows linked CRM contact + last activities; create-link button when none
- **AC:** ticket → CRM activity in one click; the activity references the ticket id.

### A5 — Subscription management for staff · **Should · 5 SP**
*As staff, I can see and change what a client pays for — and the plan actually gates something.*

**To-dos**
- [ ] Audit what tier currently enforces (subscription module + guards) — document in `docs/architecture/tier-enforcement.md`
- [ ] Client-360: subscription card (plan, period, source: manual/Stripe) + change-plan action (audited)
- [ ] Reconcile the **billing page** (client side) to real subscription data — kills one fabricated page (B2 overlap)
- **AC:** staff plan change reflects on the client's billing page immediately.

---

## EPIC B — Fully-Usable Gap Closing (client side)

*As a client, nothing I can click lies to me or leads nowhere.*

### B1 — Navigation integrity · **Must · 5 SP** · ✅ shipped PR #55 (125→0; CI file is git-ignored, checker via `npm run check:routes`)
**To-dos**
- [x] Script `scripts/check-routes.ts`: build route map from `app/[locale]/**/page.tsx`, extract `router.push`/`href` targets, fail CI on unknown targets (allowlist for dynamic)
- [x] Fix the ~94 broken targets by policy: **create** the page (if backend exists), **retarget** (if wrong path), **remove** (if aspirational)
- [x] Sidebar/global nav: every entry resolves; add missing-but-real pages to nav
- **AC:** route-checker passes clean and runs in CI.

### B2 — Data honesty (remaining fabricated pages) · **Must · 8 SP** · ✅ shipped PR #60 — 15 pages rewired in parallel (compliance, quality, GDPR, AI assistant, billing, monitoring, security, notifications, workflow, HSE, marketing audiences/analytics, integrations, partner detail, e-invoice validation, dashboard charts) · ✅ shipped PR #60 (15 pages rewired by parallel agents; all consortium honest-data blockers + majors)
**To-dos**
- [x] `compliance/page.tsx` — hardcoded ANAF statuses → real `saft-d406` dashboard/deadlines endpoints
- [x] `billing` page → real subscription data (with A5)
- [x] `monitoring` page → real health endpoint or honest „not configured" state
- [x] marketing **audiences** tab → real endpoint or honest empty-state + „în curând"
- [x] `quality` module list — calls are commented out → wire or honest state
- [x] dashboard cash-flow/VAT charts: fabricated fallbacks removed (honest empty/error states); analytics page still to review
- **AC:** zero pages render invented business data; every number on screen traces to an API.

### B3 — Core journey completion · **Must · 8 SP** (promoted by C1) · ✅ e-Factura leg shipped PR #59; company-profile fields (regCom/IBAN/CAEN) still open
**To-dos**
- [ ] Company-profile setup UI (CUI, address, IBAN, CAEN) — the data D406/e-Factura require; surface completeness on dashboard
- [x] ANAF/SPV connect flow surfaced in settings (Integrations › Conectează → real ANAF authorize URL); setup banner on every dashboard page while CUI/SPV missing
- [x] First-run onboarding checklist driven by real state (/auth/profile, /spv/status, invoices, partners) with links to each step
- **AC:** fresh account reaches a submittable e-Factura invoice guided only by the UI.

### B4 — Session, contact, errors, i18n · **Should · 5 SP**
**To-dos**
- [ ] Global fetch interceptor: 401 → redirect to login with return-URL (no more silent dead pages after 30-min expiry)
- [ ] /contact form posts to the ticket endpoint (with A2)
- [ ] Standardize error toasts: what failed + what to do (RO)
- [ ] Fix RO/EN mixing on top-10 most-visited pages
- **AC:** expired session lands on login, not on a blank dashboard.

---

## EPIC C — Consortium fold-in & ops tail

### C1 — Fold in consortium findings · **Must · 2 SP** · ✅ done 2026-08-22 (see section below)
- [x] When `wf_5a18e6ba-e30` completes (or salvage from journal if killed): merge the 7 experts' gap lists into this backlog; re-rank stories; log deltas in reqlog/ledger
- **AC:** every consortium blocker is either in a story above or explicitly parked with a reason.

### C2 — Monitoring actually running · **Could · 3 SP**
- [ ] Pick the lightest real option (uptime-kuma container or cron + Telegram alert on health-check failures) — the defined-but-not-running Prometheus stack stays parked
- **AC:** a dead backend alerts within 5 minutes.

### C3 — certainty-engine stuck-document recovery · **Could · 2 SP**
- [ ] Startup sweep + periodic job: documents `processing` > 30 min → `error` with honest message + retry button
- **AC:** no document stuck in „processing" forever after an API restart.

---

## Definition of Done (every story)
tsc clean · Jest green (new logic covered) · PR merged (`feature/req-049-*`) ·
deployed · verified live with curl/UI · reqlog + ledger updated · no new
fabricated data introduced.

## Dependencies & risks
- **User-side (blocking parts of A5/B3):** production Stripe keys; real `ANTHROPIC_API_KEY` (certainty); REQ-037 decision (fabricated executives on /about).
- Host-restart risk on the consortium workflow → salvage playbook (journal + agent transcripts) is proven.
- Ticket model is new schema — migration must be additive only.

## Sequencing
Week 1: A1 → A2 → A3 (the back-office exists) + C1 when consortium lands.
Week 2: B1 → B2 (usability Musts), then A4, A5, B3, B4; C2/C3 if capacity remains.


---

## C1 — Consortium fold-in (2026-08-22)

Review `wf_5a18e6ba-e30` completed: **7 experts · 93 gaps · 31 blockers · 50 majors**
(raw: `docs/sprints/consortium-req049-raw.json`). Verdict that changes the plan:
**the sprint story is currently blocked by the core journey itself** — registration,
session, onboarding, company data, and e-Factura submission each have a dead end.
Re-ranking accordingly.

### Re-ranked / new stories

**B3 → Must (was Should) — Core journey completion** — absorbs the ux-journey blockers (PR #59 closes the e-Factura ones; the B5/B6 ones were closed in #57/#58):
- [x] **[blocker]** Registration stores an undefined token (access_token vs accessToken) — `AuthContext.tsx`
- [x] **[blocker]** Onboarding completion POSTs to a non-existent route without auth — `page.tsx`
- [x] **[blocker]** Company/CUI settings call /api/v1/auth/profile which does not exist — `page.tsx`
- [x] **[blocker]** Onboarding writes CUI to Organization but e-Factura reads User.cui — `onboarding.service.ts`
- [x] **[blocker]** 'Trimite la SPV' buttons hit routes that do not exist; real submit service is not exposed — `page.tsx`
- [x] **[blocker]** SPV OAuth/status endpoints crash because req.user.userId is never set — `spv.controller.ts`
- [x] **[blocker]** EfacturaService uses an unset global ANAF_API_KEY instead of the user's SPV token — `efactura.service.ts`
- [x] **[blocker]** Forgot-password is a simulated stub; no reset-password or verify-email pages — `page.tsx`
- [x] **[blocker]** Login ignores requiresMfa – users who enabled MFA cannot log in — `AuthContext.tsx`

**B5 (new) · Must · 8 SP — Auth & session reliability** · ✅ shipped PR #57 — from i18n-ops + ux-journey:
- [x] Registration stores `result.access_token`; backend returns `accessToken` → new users logged out immediately (AuthContext)
- [x] Access token lives 15 min and is never refreshed → users kicked out mid-work; implement silent refresh (or longer TTL + refresh on 401)
- [x] Login ignores `requiresMfa` → MFA users can never log in; add the MFA step
- [x] Forgot-password is a stub; add reset-password + verify-email pages/endpoints
- [x] Identity read only from localStorage → role/tier changes invisible until re-login; use `validateSession`/`/auth/me` on load
- **AC:** a new account registers, stays logged in through a 30-min session, and survives a page reload.
- Note: backend refresh/verification tokens are in-memory Maps (lost on backend restart → users re-login after each deploy; reset links die on restart). Persisting them is a follow-up to-do: `- [ ] persist refresh + verification tokens (Redis/DB)`.

**B6 (new) · Must · 5 SP — API path integrity** · ✅ shipped PR #58
- [x] ~30 frontend calls use `/api/<module>` without `/v1` → nginx routes them to Next.js → 404 on load (fix + add a lint rule/checker like B1)
- [x] Onboarding completion and ANAF lookup call paths that don't exist; onboarding writes `Organization.cui` but e-Factura reads `User.cui` — unify
- [x] Company/CUI settings call `/auth/profile` which doesn't exist
- [x] Default-locale (unprefixed RO) URLs break login redirects (RO users land on `/en/login`)
- [x] `/ws` endpoint doesn't exist → dashboard permanently "Reconectare…"
- **AC:** zero frontend calls outside `/api/v1/*`; onboarding completes; settings save company data that e-Factura then uses.

**A4 → Must (was Should) — CRM as staff tooling** — crm-tools blockers must be verified first (expert read `contacts.service.ts`):
- [ ] **[blocker]** All CRM data lives in process memory and is lost on every backend restart — `contacts.service.ts`
- [ ] **[blocker]** CRM is scoped by req.user.tenantId, which is never set → every tenant shares one CRM — `contacts.controller.ts`
- [ ] **[blocker]** Creating a deal from the UI always fails (pipeline 'default-pipeline' never exists) — `page.tsx`
- [ ] **[blocker]** Activities created from the UI are not attached to any contact/deal and cannot be listed or completed — `page.tsx`

**A5 → Must (was Should) — Subscriptions** — billing-subs blockers:
- [ ] **[blocker]** POST /subscription/upgrade grants paid tiers with zero payment, to any org member — `subscription.controller.ts`
- [ ] **[blocker]** Subscription page spins forever for users with an organization (TypeError on plan object) — `page.tsx`
- [ ] **[blocker]** No real payment processing exists — Stripe is simulated, unsigned, in-memory, unconfigured — `payment-gateway.service.ts`

**B2 — Data honesty** gains the honest-data blockers (plus the 11 majors as to-dos) — ✅ all 15 closed in PR #60:
- [x] **[blocker]** Compliance page renders 100% fabricated ANAF/GDPR/SOC2 statuses and declarations — `page.tsx`
- [x] **[blocker]** Quality dashboard never calls the backend — every tab shows an error/empty state even when data exists — `page.tsx`
- [x] **[blocker]** GDPR console shows fabricated data-subject requests with fake personal names instead of real DSRs — `page.tsx`
- [x] **[blocker]** AI assistant fabricates financial 'analysis' of the user's data via setTimeout + canned text — `page.tsx`

**A1 follow-ups (admin-backend majors, all Should):**
- [ ] **[major]** Staff tier change only updates User.tier — org tier, limits and /subscription/status unchanged — `admin-clients.service.ts:162`
- [ ] **[major]** Staff cannot read a client's audit trail — /audit is tenant-scoped to the caller's own org — `audit.controller.ts:38`
- [ ] **[major]** Staff replies never reach the client — no email or in-app notification on ticket events — `support.service.ts:232`
- [ ] **[major]** Impersonation always returns 403 (role list is lowercase) and issues no token even if it passed — `user-impersonation.service.ts:66`
- [ ] **[major]** Any authenticated user can create/toggle/delete platform feature flags and overrides — `feature-flags.controller.ts:29`
- [ ] **[major]** /admin/analytics/* returns fabricated Math.random revenue/user metrics to staff — `platform-analytics.service.ts:135`
- [ ] **[major]** No way to suspend/reactivate a client, force password reset, resend verification, or see last login — `schema.prisma:115`
- [ ] **[major]** Client search is user-only; no organization list/search and 360 shows only the first membership — `admin-clients.service.ts:145`
- [ ] **[minor]** Role change has no safeguards: admin can demote self or the last ADMIN, no reason required — `admin-clients.controller.ts`
- [ ] **[minor]** Staff cannot open a ticket on behalf of a client (phone/email requests) despite source 'staff' being modelled — `support.controller.ts`
- [ ] **[minor]** ContactSubmission table is write-only and diverges from the ticket copy — `public-contact.service.ts:31`

### Already closed by B1 (PR #55) — frontend-nav
All 6 frontend-nav blockers (signup CTA, invoice delete/send-email, HR hub actions, period close/lock/reopen*, CRM/partner deletes, contracts hub) and the 8 majors are addressed by the B1 codemod — *period close/lock/reopen are honest notices pending the period-closing IDOR fix (controller takes `:userId` from path), now a B3 to-do.

### Capacity note
Must is now ~75 SP against ~55 capacity. Order of execution: **B5 → B6 → B3** (journey, in
that dependency order) → B2 → A4/A5 → remaining. Anything that slips moves to the next
sprint explicitly — it does not silently become "done".

### B2 follow-ups surfaced by the rewires (backend truths the pages now expose honestly)
- [ ] **AI assistant**: `/ai-assistant` backend is canned regex text + in-memory Map; a real Grok path exists (`POST /ai/ask`, `grok-conversation.service`) — route the assistant through it and persist conversations (CLAUDE.md mandates Grok).
- [ ] **In-memory stores** (lost on restart, not shared across slices): CRM, workflow engine, notification center, integrations hub, billing, HSE, quality, API-gateway keys, refresh/verification tokens, compliance calendar, SOC 2 — persist the ones that matter first (refresh tokens, CRM, notifications).
- [ ] `GET /hse/dashboard` hardcodes sample KPIs; `/compliance/anaf-status/calendar` hardcodes events; `/gdpr/consent-log` simulated; `/marketing/email/campaigns/:id/analytics` timeline synthetic — replace or remove.
- [ ] Validator error messages (EN) → Romanian; `/security/events` per-user filter; failed password logins not audited.
- [x] Fixed alongside: `GET /invoices?partnerId=`; subscription routes 404 (RO message) without org instead of 500; DSR mapper exposes name/email; notification-center item ownership checks; automation `:id` route no longer shadows `/segments`,`/stats`; compliance-calendar guarded + org-scoped; `req.user.tenantId` now set (CRM/hub/pipeline scoping); BigInt JSON serializer.
